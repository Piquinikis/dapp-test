{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna clean -y && lerna bootstrap --use-workspaces"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
